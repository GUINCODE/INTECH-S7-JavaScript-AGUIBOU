let array=[1,2,8,9]
for  (let index = 0; index < array.length; index++) {
    if (array[index]==10) {
      console.log("la taille du tableau est: "+array.length);
    }
}

//Qu'est ce qui sera affiché sur l'ecran
/**
 * a)-Rien ne sera affiche
 *  b)- la taille du tableau est: 4
 * c)- la taille du tableau est: undefined
 * d)- Il y aura une erreur
 * 
 */

